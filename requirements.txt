# Sea-quenced pipeline dependencies
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4      # provides cluster.HDBSCAN
scipy>=1.10            # chi-squared novelty threshold
matplotlib>=3.7
torch>=2.0             # self-supervised contrastive encoder

# Install:  pip install -r requirements.txt
# (CPU-only torch is fine; no GPU required.)
